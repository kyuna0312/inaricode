# `minimal-review` (example)

Reference **declarative** skill: **`skill.yaml`** + **`prompt.md`**.

- Validate the manifest against **`../../skill.manifest.schema.json`** (see **`packages/skills/README.md`**).
- **`inari doctor`** prints the path to **`packages/skills/examples`** when you run from a monorepo checkout.

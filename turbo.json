{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**", "*.node"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
